# Retired — use Scalar + ReDoc

Insomnia design/collection sync is no longer used.

Hosted docs:

- `/scalar` — interactive try-it-out (Scalar)
- `/redoc` — read-only reference (ReDoc)
- `/openapi/v1.json` — OpenAPI document

See `docs/api/README.md`.
