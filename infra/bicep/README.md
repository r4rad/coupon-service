# Bicep (assignment — in scope)

This folder will hold the Azure resources for the demo: App Service **F1**, API Management **Consumption**, Application Insights, and optional Key Vault.

The Azure DevOps pipeline deploys **only** this stack (`az deployment group`).

See [docs/solution-architecture.md](../../docs/solution-architecture.md).
